/**
 * EmpresaController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `EmpresaController.id()`
   */
  id: async function (req, res) {
    return res.json({
      todo: 'id() is not implemented yet!'
    });
  },

  /**
   * `EmpresaController.nombre()`
   */
  nombre: async function (req, res) {
    return res.json({
      todo: 'nombre() is not implemented yet!'
    });
  },

  /**
   * `EmpresaController.ruc()`
   */
  ruc: async function (req, res) {
    return res.json({
      todo: 'ruc() is not implemented yet!'
    });
  },

  /**
   * `EmpresaController.direccion()`
   */
  direccion: async function (req, res) {
    return res.json({
      todo: 'direccion() is not implemented yet!'
    });
  },

  /**
   * `EmpresaController.telefono()`
   */
  telefono: async function (req, res) {
    return res.json({
      todo: 'telefono() is not implemented yet!'
    });
  },

  /**
   * `EmpresaController.correo()`
   */
  correo: async function (req, res) {
    return res.json({
      todo: 'correo() is not implemented yet!'
    });
  },

  /**
   * `EmpresaController.personas()`
   */
  personas: async function (req, res) {
    return res.json({
      todo: 'personas() is not implemented yet!'
    });
  }

};

